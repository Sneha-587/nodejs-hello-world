name: Check SonarQube Connectivity

on:
  push:
    branches:
      - main

jobs:
  check-connectivity:
    runs-on: ubuntu-latest

    steps:
      - name: Check SonarQube Connectivity
        run: |
          curl -v http://192.168.8.7:9000

      # Add more steps for your actual build/test process
